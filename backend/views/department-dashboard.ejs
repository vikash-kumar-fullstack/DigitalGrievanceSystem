<!DOCTYPE html>
<html>
<head>
  <title>Department Dashboard</title>
  <!-- <style>
    body {
      margin: 0;
      font-family: Segoe UI, sans-serif;
      background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
      color: #fff;
    }

    .container {
      max-width: 1000px;
      margin: 60px auto;
      background: rgba(255,255,255,0.08);
      backdrop-filter: blur(12px);
      padding: 40px;
      border-radius: 18px;
      box-shadow: 0 25px 60px rgba(0,0,0,0.4);
    }

    h2 {
      text-align: center;
      margin-bottom: 30px;
    }

    .card {
      background: rgba(255,255,255,0.12);
      padding: 20px;
      border-radius: 12px;
      margin-bottom: 20px;
    }

    .status {
      font-weight: bold;
      margin-top: 10px;
    }

    select {
      padding: 8px;
      border-radius: 8px;
      border: none;
    }

    button {
      padding: 8px 14px;
      border-radius: 8px;
      border: none;
      background: #00c6ff;
      font-weight: bold;
      cursor: pointer;
      margin-left: 10px;
    }
  </style> -->
  <link rel="stylesheet" href="/css/style.css">
</head>

<body class="department-body">
  <div class="container">
    <h2>Department Complaints</h2>

    <% if (complaints.length === 0) { %>
      <p>No complaints assigned.</p>
    <% } %>

    <% complaints.forEach(c => { %>
      <div class="card">
        <p><strong>Message:</strong> <%= c.message %></p>
        <p class="status">Status: <%= c.status %></p>

        <form method="POST" 
        action="/department/update-status/<%= c._id %>" 
        enctype="multipart/form-data">

        <select name="status" onchange="toggleResolved(this, '<%= c._id %>')">
            <option>Pending</option>
            <option>In Progress</option>
            <option>Resolved</option>
        </select>

        <div id="resolved-box-<%= c._id %>" style="display:none; margin-top:10px;">
            <input type="file" name="proofDocument" accept="application/pdf" />
            <textarea name="remarks" placeholder="Enter remarks..."></textarea>
        </div>

        <button style="padding: 10px;" type="submit">Update</button>
        </form>
      </div>
    <% }) %>
  </div>
  <script src="/socket.io/socket.io.js"></script>

<script>
  const socket = io();

  // join personal room
  socket.emit("join", "<%= user._id %>");

  socket.on("newNotification", (data) => {
    // 🔥 SIMPLE POPUP
    alert("🔔 " + data.message);

    // optional: reload page
    // location.reload();
  });
</script>
</body>
<script>
function toggleResolved(select, id) {
  const box = document.getElementById("resolved-box-" + id);
  if (select.value === "Resolved") {
    box.style.display = "block";
  } else {
    box.style.display = "none";
  }
}
</script>
</html>